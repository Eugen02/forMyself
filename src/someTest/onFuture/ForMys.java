package someTest.onFuture;

import java.util.*;

public class ForMys {
    public static void main(String[] args) {

    Map<Integer,String> map = new TreeMap<>();
    Set<String> set= new  TreeSet<>();
    set.add("S");
    set.add("S");
    Iterator<String> uter = set.iterator();
        set.add("S2");
        set.add("S3");
        while (uter.hasNext()){
            System.out.println(uter.next());
        }
        ArrayList<String> se = new ArrayList<>();

    }
}
